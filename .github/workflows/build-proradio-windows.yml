name: Compilar Pró Rádio Windows (Recomendado)

on:
  workflow_dispatch:

jobs:
  run-windows-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "proradio"
      windows-only: true
